# 踩坑日志

## Bug #2: 服务器单线程阻塞导致请求全部超时 (2026-06-09)

### 现象
浏览器显示「无法访问此站点」，端口 8765 处于 LISTENING 状态但所有 HTTP 请求均超时。
etstat 显示大量 CLOSE_WAIT 连接。

### 根因
http.server.HTTPServer 默认是单线程的。serve_forever() 在主线程中循环调用 ccept() → handle_request()，一次只处理一个请求。

当内嵌浏览器连接后没有正常关闭（如导航到其他页面、刷新、或浏览器进程异常），TCP 连接进入 CLOSE_WAIT 状态。此时服务端的 socket 未被释放，handle_request() 无法返回，导致主线程永久阻塞，所有后续请求全部排队超时。

### 修复
将 HTTPServer 替换为多线程版本：

`python
from socketserver import ThreadingMixIn

class ThreadingHTTPServer(ThreadingMixIn, HTTPServer):
    daemon_threads = True  # 主线程退出时自动清理工作线程
`

每个 HTTP 请求在独立线程中处理，一个连接卡死不会影响其他请求。

### 临时解决
杀掉进程重启可以临时恢复，但再次访问后又会复现。


## Bug #1: 前端事件重放导致状态混乱 (2026-06-08)

### 现象
搜索按钮点击后无反应，或闪现旧结果后才出现新结果。

### 根因
1. search() 每次将 lid 重置为 0，导致 poll() 拉取服务器全部历史事件（最多 200 条）
2. poll() 不区分 search/scrape 模式，搜索时会误处理旧的 scrape_done、ideo_done 等事件
3. inishS() 用 setTimeout 延迟 8 秒清理轮询间隔，容易与后续操作冲突

### 修复
- 用统一 mode 变量替代分散的 searching/scraping
- search() 不再重置 lid，页面加载时同步服务端最新事件 ID
- poll() 按 mode 过滤事件类型
- inishS()/s() 立即清理轮询间隔


## Bug #3: JS语法错误导致整个脚本无法执行 (2026-06-09)

### 现象
搜索按钮点击无反应，输入关键词后点搜索也没有任何变化。后端 API 单独测试全部正常。

### 根因
inishS() 和 s() 两个函数体内残留了 }},8000)} 垃圾代码：

`js
function finishS(){...clearInterval(pid);pid=null;}},8000)}  // SyntaxError!
function fs(){...clearInterval(pid);pid=null;}},8000)}        // SyntaxError!
`

这是上一步修复时用正则表达式 
'function finishS\(\)\{[^}]+\}' 替换函数体失败导致的。因为原始函数体内含多层嵌套的 {}（setTimeout 回调、if 语句），[^}]+ 只匹配到第一个 } 就停止了，导致只替换了函数体的前半部分，后半部分残留下来变成垃圾代码。

浏览器解析 JS 时遇到 SyntaxError，整个 <script> 块全部作废——所有函数（search、poll、
ender等）都没有被定义，点击搜索按钮完全没有反应。

### 修复
放弃正则替换方案，完整重写 index.html，确保每个函数体完整无误。

### 教训
- 不要用正则表达式替换嵌套代码块，尤其是 minified JS
- 生成代码时宁可重写也不要依赖字符串替换
- 每次修改 HTML 后应检查 JS 语法（至少 grep 常见垃圾模式）

---

## 修复汇总

当前已修复的所有 bug：

| Bug | 问题 | 修复 |
|-----|------|------|
| #1 | 前端事件重放（lid=0） | mode 变量 + 事件过滤 + 不重置 lid |
| #2 | 单线程服务器卡死 | ThreadingHTTPServer |
| #3 | JS 语法错误导致全局失效 | 重写 index.html，无残留代码 |
